/** Outer wrapper for primary app pages (width and vertical padding). */
export const pageShellClass =
  "mx-auto flex w-full max-w-lg flex-1 flex-col px-4 py-16";

/** Primary page title — use a single <h1> with this class per route. */
export const pageTitleClass =
  "text-xl font-semibold tracking-tight text-zinc-900 dark:text-zinc-50";

/** Stacks title, intro copy, and top navigation with consistent rhythm. */
export const pageHeaderClass = "space-y-6";

/** Gap between the page header block and the main content below it. */
export const pageBodyGapClass = "mt-10";
